<%- include('../partials/head', { pageTitle: 'Pengaturan', page: 'settings' }) %>

<div class="grid-2">
  <div class="card">
    <div class="card-header">
      <div>
        <h3>Logo Perusahaan</h3>
        <p>Unggah logo baru (PNG/JPG/WEBP, max 2MB).</p>
      </div>
    </div>
    <div class="logo-preview">
      <% if (company.logo_data || company.logo_path) { %>
        <img src="<%= company.logo_data ? company.logo_data : '/' + company.logo_path %>" alt="Logo" />
      <% } else { %>
        <div class="empty-state">Belum ada logo</div>
      <% } %>
    </div>
    <form method="post" action="/settings/logo" enctype="multipart/form-data" class="form-stack">
      <label>
        File Logo
        <input type="file" name="logo" accept="image/png,image/jpeg,image/webp" required />
      </label>
      <button class="btn primary" type="submit">Simpan Logo</button>
    </form>
  </div>

  <div class="card">
    <div class="card-header">
      <div>
        <h3>Backup Database</h3>
        <p>Unduh backup Excel lengkap (data master & transaksi).</p>
      </div>
    </div>
    <p class="muted">File berisi Divisi, Jenis Barang, Item, Stock Awal, Transaksi, Adjustment, dan User.</p>
    <a class="btn ghost" href="/backup/company">Download Backup Excel</a>
  </div>

  <div class="card">
    <div class="card-header">
      <div>
        <h3>Test Auto Backup</h3>
        <p>Kirim backup otomatis ke email User Utama sekarang.</p>
      </div>
    </div>
    <p class="muted">
      Sistem akan mengirim 3 file: Backup Excel, Excel Transaksi, dan Excel Laporan.
      Pastikan SMTP sudah diisi di Render.
    </p>
    <form method="post" action="/backup/test">
      <button class="btn primary" type="submit">Kirim Backup Sekarang</button>
    </form>
  </div>
</div>

<%- include('../partials/foot') %>
