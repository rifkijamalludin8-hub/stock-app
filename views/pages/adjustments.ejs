<%- include('../partials/head', { pageTitle: 'Adjustment', page: 'adjustments' }) %>

<div class="grid-2">
  <div class="card">
    <div class="card-header">
      <div>
        <h3>Adjustment Stock</h3>
        <p>Perbaiki selisih stock. Hanya user.</p>
      </div>
    </div>
    <form method="post" action="/adjustments" class="form-grid" enctype="multipart/form-data">
      <label>
        Tanggal
        <input name="adj_date" type="date" value="<%= today %>" />
      </label>
      <label>
        Item
        <select name="item_id" required>
          <option value="">Pilih Item</option>
          <% items.forEach((item) => { %>
            <option value="<%= item.id %>"><%= item.group_name %> - <%= item.name %> - <%= item.expiry_date || '-' %></option>
          <% }) %>
        </select>
      </label>
      <label>
        Qty Delta
        <input name="qty_delta" type="number" step="0.01" placeholder="-5 atau 10" required />
      </label>
      <label class="full">
        Catatan
        <textarea name="note" rows="2"></textarea>
      </label>
      <label class="full">
        Bukti Adjustment (JPG/PNG/WEBP/PDF)
        <input type="file" name="proof" accept="image/*,application/pdf" />
      </label>
      <button class="btn primary" type="submit">Simpan</button>
    </form>
    <div class="section-block">
      <div class="section-title">Import Excel</div>
      <p class="muted">
        Kolom minimal: Item, Qty Delta (atau Qty). Tanggal opsional (jika kosong memakai tanggal hari ini).
        Item bisa berupa label "Jenis - Nama - Exp" atau isi kolom terpisah: Jenis Barang, Nama Item, Expired.
      </p>
      <form method="post" action="/adjustments/import" class="form-grid" enctype="multipart/form-data">
        <label class="full">
          File Excel (.xlsx)
          <input type="file" name="file" accept=".xlsx" required />
        </label>
        <button class="btn ghost" type="submit">Import Adjustment</button>
      </form>
    </div>
    <div class="export-row">
      <a class="btn ghost" href="/export/adjustments?format=xlsx">Export Excel</a>
      <a class="btn ghost" href="/export/adjustments?format=pdf">Export PDF</a>
    </div>
  </div>

  <div class="card">
    <div class="card-header">
      <div>
        <h3>Riwayat Adjustment</h3>
        <p>50 adjustment terakhir.</p>
      </div>
    </div>
    <div class="table-wrapper">
      <table>
        <thead>
          <tr>
            <th>Tanggal</th>
            <th>Item</th>
            <th>Qty</th>
            <th>Bukti</th>
            <th>Catatan</th>
          </tr>
        </thead>
        <tbody>
          <% adjustments.forEach((a) => { %>
            <tr>
              <td><%= a.adj_date %></td>
              <td><%= a.item_label %></td>
              <td><%= a.qty_delta %></td>
              <td>
                <% if (a.proof_path) { %>
                  <a class="link" href="/<%= a.proof_path %>" target="_blank" rel="noopener">Lihat</a>
                <% } else { %>
                  -
                <% } %>
              </td>
              <td><%= a.note || '-' %></td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
  </div>
</div>

<%- include('../partials/foot') %>
