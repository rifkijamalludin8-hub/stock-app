<%- include('../partials/auth-head', { pageTitle: 'Akses Ditolak' }) %>

<h1 class="auth-title">Akses Ditolak</h1>
<p class="auth-sub">Maaf, kamu tidak punya akses untuk halaman ini.</p>

<a class="btn primary" href="/">Kembali ke Dashboard</a>

<%- include('../partials/auth-foot') %>
